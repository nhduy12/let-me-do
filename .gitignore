# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Per-project autopilot artifacts — never commit (the plugin's commit pipeline already excludes these,
# but a manual `git add` could pick them up)
.lmd/

# Local env / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
